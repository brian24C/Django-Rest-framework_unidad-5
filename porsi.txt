    "DEFAULT_THROTTLE_CLASSES": [
        'rest_framework.throttling.AnonRateThrottle', # anonimos
        'rest_framework.throttling.UserRateThrottle', # usuarios logueados
        'rest_framework.throttling.ScopedRateThrottle'
    ],
    "DEFAULT_THROTTLE_RATES": {
        'anon': '4/day',
        'user': '8/day',
        'generate_code': '1/minute'
    }







------------------------------------
------------------------------------
------------------------------------
------------------------------------

urls.py servicios



from rest_framework import routers
from .api import ServicioViewSet, PaymentViewSet, ExpiredView,ExpiredViewSet
from django.urls import path, re_path, include
from rest_framework_simplejwt.views import (
    TokenObtainPairView,
    TokenRefreshView,
)

from streamingPagos.api import PagoViewSet


router = routers.DefaultRouter()
router.register('readService', ServicioViewSet, basename="service")
router.register('payment', PaymentViewSet, basename="PaymentViewSet")
router.register('expired', ExpiredViewSet, basename="ExpiredViewSet")

router.register(r'streaming', PagoViewSet, basename='pagos')


from versionedService.v1.router import urlpatterns as api_v1
from versionedService.v2.router import urlpatterns as api_v2


urlpatterns = [
    #path("expired2/", ExpiredView.as_view(), name="expired_"),
    #path("expired/", ExpiredViewSet.as_view(), name="expired2"),
    #re_path(r'^expired/', ExpiredViewSet.as_view(), name="expired2"),
    path('general/', include((router.urls))),

    path('v1/', include((api_v1))),
    path('v2/', include((api_v2))),
    
]

urlpatterns += router.urls


Me identifico con los gatos porque son idependientes y no se dejan sorprender por nada y siempre van con la cabeza en alto.




Paramount
Paramount Plus es un servicio de streaming por suscripción similar a Netflix o Disney Plus que ofrece más de 5000 horas de entretenimiento en series, programas, shows y películas.
Logo Paramount

Start
Es un nuevo servicio de streaming, que tiene contenido de deportes, series, películas y series animadas.
Logo Start

Amazon
Ofrece streaming ilimitado de películas y series de televisión para miembros que paguen o tengan una prueba gratis en los Estados Unidos y Puerto Rico. Para obtener más información, ve a Prime Video. Con Amazon Channels, puedes ver tus programas y películas favoritos de los canales HBO, SHOWTIME y STARZ.
logo Amazón

Netflix
Es un servicio de streaming por suscripción que les permite a sus miembros ver series y películas en un dispositivo con conexión a internet
logo netflixt